/**
 * App Router navigation that starts the top progress hairline on push/replace.
 * Keep `usePathname` / `useSearchParams` on `next/navigation`.
 * `router.refresh()` does not need this hook.
 */
export { useRouter } from "nextjs-toploader/app";
