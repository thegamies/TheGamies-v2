/**
 * Cover grid for TGA ballots and admin / show room.
 * Min card ~10rem so phones wrap to 2, desktop lands 5–6 (same idea as
 * standings / games browse, without a fixed column count).
 */
export const tgaNomineeGridClass =
  "grid grid-cols-[repeat(auto-fill,minmax(10rem,1fr))] gap-3 sm:gap-4";
